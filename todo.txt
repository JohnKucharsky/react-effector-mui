+ icon button icon and button size to look at
+ check icon to color picker
+ on click detailed info
+ Key extends keyof any to fix
+ accordion for posts and table inside for comments
- remove duplications and unnecessary elements
- remove factories
- remove lodash with custom functions

+ max width table, and max width header top
+ dark mode
+ handleClose on create
+ phone field to do
+ divide create and delete
+ yup schema
+ add refresh button everywhere, refresh, tooltip it will reset all changes
+ pick app color
+ fields spacing
+ header

